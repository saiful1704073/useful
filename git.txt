at first

1.git init
2.git add
3.git commit -m " "
4.git remote add origin (use url not ssh)
5.git push origin master


when added other file or change something

1.git add
2.git commit -m " "
3.git remote -v
4.git push origin master


cloning a git folder

1.git clone (https url)


git branch (name) #creating branch 
git chekout (name) #switching branch
git merge (name) #merging branch
git push origin master
git log #seeing the changes

###krish naik video